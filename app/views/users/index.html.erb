<ul>
  <%= @users.each do |user| %>
    <li class="card">
      <div class="img">
        <%= render partial: "shared/user_card", locals: { user: user } %>
      </div>
      <button class="btn_user">
        <%= link_to "Access", user_path(user) %>
      </button>
    </li>
  <% end %>
</ul>
