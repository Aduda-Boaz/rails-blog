<ul>
  <%= @users.each do |user| %>
    <li class="card">
      <div class="img">
        <%= image_tag(user.photo, width: "130px") %>
      </div>
      <div class="user_card">
        <div class="user_name"><%= user.name %></div>
        <div class="user_posts">Posts<%= user.post_count %></div>
      </div>
    </li>
  <% end %>
</ul>
